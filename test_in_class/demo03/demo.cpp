#include <cstdio>
#include <iostream>
#include <cstring>
#include <vector>
#include <unordered_map>
#include <map>
#include <set>
#include <queue>
#include <cmath>

using namespace std;

const int N = 10010;

double arr[N];

int main(){
    // int n = 6;
    // double ans, avg;
    // for(int i = 0;i < n;i++){
    //     cin >> arr[i];
    //     avg += arr[i];
    // }
    // avg /= 6;
    // for(int i = 0;i < n;i++){
    //     ans += pow(arr[i] - avg, 2);
    // }
    // ans = sqrt(ans / (n * (n - 1)));
    // cout << ans;

    cout << 331.45 * sqrt(1 + 27.5 / 273);
    return 0;
}
